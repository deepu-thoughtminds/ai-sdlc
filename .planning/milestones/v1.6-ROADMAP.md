# Milestone v1.6: Context-Aware Codebase Scanning

**Status:** ✅ Shipped 2026-06-22
**Phases:** 18-21 (4 phases, 8 plans, 9 requirements)
**Git range:** f6cee7c (18-01) → c5501b4 (21-01), 2026-06-21 → 2026-06-22

Give every pipeline stage accurate, real codebase context by scanning the project repo at onboarding, committing a structured summary to the repo, and feeding it into description and architecture generation — so LLM outputs reference actual file paths and patterns instead of guessing.

---

### Phase 18: Codebase Scan Service

**Goal**: When a project is onboarded with a `github_repo` field, the agent automatically clones the repo, walks the directory tree with targeted file reads, and commits a structured `.hermes/codebase.md` summary to the main branch — no manual step and no LLM required for the scan itself
**Depends on**: Phase 17
**Requirements**: SCAN-01, SCAN-02, SCAN-03 — all shipped

Plans:
- [x] 18-01-PLAN.md — Async codebase scan service (SCAN-02, SCAN-03): walk GitHub tree, select ≤25 key files, build .hermes/codebase.md, commit via Contents API PUT
- [x] 18-02-PLAN.md — Trigger wiring + tests (SCAN-01): convert create_project to async, schedule background scan task, unit test suite with respx mocks
- [x] 18-03-PLAN.md — Gap closure: regression tests for PipelineState commit + asyncio.create_task scan trigger in test_projects.py (SCAN-01)

### Phase 19: Snapshot Refresh & Read Fallback

**Goal**: The codebase snapshot stays current after each merge and the pipeline never crashes when the snapshot is absent
**Depends on**: Phase 18
**Requirements**: SNAPSHOT-01, SNAPSHOT-02 — all shipped

Plans:
- [x] 19-01-PLAN.md — Post-merge codebase re-scan hook in merge_pipeline.run() (SNAPSHOT-01)
- [x] 19-02-PLAN.md — Standalone get_codebase_snapshot() reader with graceful 404/error fallback (SNAPSHOT-02)

### Phase 20: Describe Pipeline Context

**Goal**: Story elaborations produced by `@jarvis describe` reference real module names and file paths from the codebase instead of generic placeholders
**Depends on**: Phase 19
**Requirements**: DESCCTX-01, DESCCTX-02 — all shipped

Plans:
- [x] 20-01-PLAN.md — Replace get_codebase_summary with get_codebase_snapshot in describe_pipeline; update tests; add snapshot content assertion (DESCCTX-01, DESCCTX-02)

### Phase 21: Architecture Pipeline Context

**Goal**: Architecture writeups produced by `@jarvis architecture` reference actual existing components, file paths, and integration points rather than invented structure
**Depends on**: Phase 20
**Requirements**: ARCHCTX-01, ARCHCTX-02 — all shipped

Plans:
- [x] 21-01-PLAN.md — Thread codebase snapshot through architecture_pipeline.run() and complexity_classifier (ARCHCTX-01, ARCHCTX-02)

---

## Milestone Summary

**Decisions:**
- Codebase scan uses git clone + directory tree walk + targeted file reads — no LLM; output is a structured markdown file committed directly to main as `.hermes/codebase.md`
- Scan triggered automatically on project onboarding when `github_repo` is saved — no separate user action
- Snapshot refresh hooks into `merge_pipeline.py` post-merge path; read path degrades gracefully when the file is absent
- `describe_pipeline.py` and `architecture_pipeline.py` both read `.hermes/codebase.md` via GitHub API before LLM calls

**Deferred to future milestones:**
- On-demand refresh trigger (`@jarvis refresh codebase`)
- Diff-based incremental scan (re-scan only changed files)
- Codebase embedding / vector search

**Known gaps acknowledged at close** (see STATE.md Deferred Items): 1 open debug session, Phase 17 UAT (3 untested scenarios), Phase 15 verification gaps, 5 quick tasks of unclear/missing status — none block v1.6's own requirements, all carried forward as tech debt.
