---
phase: 13-pipeline-orchestration-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/architecture_pipeline.py
autonomous: true
requirements:
  - ARCHGEN-01
  - ARCHGEN-02
  - ARCHGEN-03
  - ARCHINT-03

must_haves:
  truths:
    - "architecture_pipeline.run() calls classify_complexity() and branches on 'small'/'complex'"
    - "For 'complex': generate_diagram() and generate_viewer_url() are called; publish_architecture() called with is_complex=True; Jira comment contains 'Multi-component feature — diagram included'"
    - "For 'simple': no diagram call; publish_architecture() called with is_complex=False; Jira comment contains 'Simple change — text architecture'"
    - "No _parse_options(), no _build_prompt(), no 'Reply approved [option name]' string anywhere in the file"
    - "PipelineState is set to status='complete' (not 'awaiting_approval') with draft_content as the final human-readable comment string"
  artifacts:
    - path: "backend/services/architecture_pipeline.py"
      provides: "Single-pass architecture pipeline with complexity branching"
      contains: "classify_complexity"
    - path: "backend/services/architecture_pipeline.py"
      provides: "No multi-option flow"
      min_lines: 80
  key_links:
    - from: "architecture_pipeline.run()"
      to: "complexity_classifier.classify_complexity()"
      via: "direct call"
      pattern: "classify_complexity"
    - from: "architecture_pipeline.run() complex branch"
      to: "drawio_service.generate_diagram()"
      via: "direct call"
      pattern: "generate_diagram"
    - from: "architecture_pipeline.run()"
      to: "confluence_client.publish_architecture()"
      via: "direct call"
      pattern: "publish_architecture"
---

<objective>
Rewrite backend/services/architecture_pipeline.py as a single-pass complexity-aware pipeline that classifies the ticket, conditionally generates a diagram, publishes to Confluence, and posts a final Jira comment. Remove all multi-option logic.

Purpose: Per ARCHGEN-01, ARCHGEN-02, ARCHGEN-03, ARCHINT-03 — the old "generate 2-3 options, reply approved [option name]" flow is replaced with a single recommended architecture, complexity-driven.

Output: A fully rewritten architecture_pipeline.py with no _parse_options(), no _build_prompt(), and no "Reply 'approved'" text anywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/architecture_pipeline.py
@backend/services/complexity_classifier.py
@backend/services/drawio_service.py
@backend/services/confluence_client.py
@backend/services/llm_router.py
@backend/models/pipeline_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite architecture_pipeline.py — new single-pass complexity-aware flow</name>
  <files>backend/services/architecture_pipeline.py</files>
  <behavior>
    - complex path: classify_complexity returns ("complex", ...) → generate_diagram called once → generate_viewer_url called → publish_architecture called with is_complex=True → comment contains "Multi-component feature — diagram included" and the Confluence URL
    - simple path: classify_complexity returns ("small", ...) → generate_diagram NOT called → publish_architecture called with is_complex=False → comment contains "Simple change — text architecture" and the Confluence URL
    - draft_content stored on PipelineState is the human-readable comment string (same as return value), not JSON
    - PipelineState row is created at the top of run() with status="running" BEFORE the LLM call (enables idempotency guard in webhook.py)
    - PipelineState.status is set to "complete" at the end (not "awaiting_approval")
    - No _parse_options(), no _build_prompt(), no "Reply 'approved [option name]'" string exists anywhere
    - Confluence failure is caught gracefully; pipeline still posts Jira comment without URL
  </behavior>
  <action>
    Replace the entire content of backend/services/architecture_pipeline.py. Do NOT preserve _build_prompt(), _parse_options(), or any "Reply 'approved [option name]'" text — per ARCHGEN-01 and ARCHINT-03 these must be fully removed.

    New module docstring must reference ARCHGEN-01, ARCHGEN-02, ARCHGEN-03, ARCHINT-03 and the threat mitigations T-04-01, T-04-03.

    Imports required (use exact names already exported by each service):
      - from services.complexity_classifier import classify_complexity
      - from services.drawio_service import generate_diagram, generate_viewer_url
      - from services.confluence_client import publish_architecture
      - from services.llm_router import route_request
      - from models.pipeline_state import PipelineState
      - from models.project import Project
      - from sqlalchemy.orm import Session
      - import logging

    Define a single async function run(project, issue_key, issue_summary, issue_description, db) -> str.

    Step 1 — Create PipelineState row immediately (status="running") and commit. This row must exist BEFORE the LLM call so webhook.py can detect a duplicate before scheduling the background task. Set state_row.complexity and state_row.complexity_rationale after classify_complexity returns.

    Step 2 — Call classify_complexity(issue_key, issue_summary, issue_description, db, project.id). Returns (complexity, rationale). complexity is "small" or "complex".

    Step 3a — Complex branch (complexity == "complex"):
      Build an LLM prompt requesting exactly six labelled sections in order: "## Summary", "## Approach", "## Component Breakdown", "## Integration Points", "## Key Decisions", "## Risks". The prompt must specify: "You are a software architect. Generate ONE recommended architecture for the following Jira ticket. Do NOT generate multiple options." Include issue_key, issue_summary, issue_description in the prompt. T-04-01: no token values in prompt.
      Call route_request("architecture", prompt). Parse the six sections from llm_output by splitting on the "## " prefix lines. Extract: summary, approach, component_breakdown, integration_points, key_decisions, risks as plain text strings.
      Extract component names: split component_breakdown on commas and newlines to get a list of strings for the diagram.
      Call generate_diagram(title=f"Architecture: {issue_key}", components=component_list, connections=[]).
      Call generate_viewer_url(diagram_xml) to get the viewer URL.
      Call await publish_architecture(project, issue_key, summary, approach, key_decisions, risks, is_complex=True, component_breakdown=component_breakdown, integration_points=integration_points, diagram_xml=diagram_xml, viewer_url=viewer_url) wrapped in try/except — on failure set page_url="".
      Build comment_text:
        f"*Architecture for {issue_key}*\n\n{summary}\n\nMulti-component feature — diagram included.\n\nConfluence: {page_url}" if page_url else f"*Architecture for {issue_key}*\n\n{summary}\n\nMulti-component feature — diagram included.\n\n(Confluence publishing unavailable)"

    Step 3b — Simple branch (complexity == "small"):
      Build an LLM prompt requesting exactly four labelled sections in order: "## Summary", "## Approach", "## Key Decisions", "## Risks". Same system instruction as complex but omitting component/integration sections.
      Call route_request("architecture", prompt). Parse four sections from llm_output.
      Call await publish_architecture(project, issue_key, summary, approach, key_decisions, risks, is_complex=False) wrapped in try/except — on failure set page_url="".
      Build comment_text:
        f"*Architecture for {issue_key}*\n\n{summary}\n\nSimple change — text architecture.\n\nConfluence: {page_url}" if page_url else f"*Architecture for {issue_key}*\n\n{summary}\n\nSimple change — text architecture.\n\n(Confluence publishing unavailable)"
      NOTE: generate_diagram must NOT be called in this branch.

    Step 4 — Update state_row: status="complete", draft_content=comment_text. Commit. Return comment_text.

    Section parser helper (_parse_sections): a module-private function that accepts llm_output (str) and a list of section names, returns a dict mapping section name to content string. Splits on lines starting with "## ". If a section is missing from the LLM output, returns "" for that key. This helper must NOT raise exceptions.

    Error handling: wrap the entire body of run() in try/except; on any unhandled exception set state_row.status="failed", commit, then re-raise.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -c "import services.architecture_pipeline as m; import inspect; src = inspect.getsource(m); assert '_parse_options' not in src, 'old multi-option code still present'; assert '_build_prompt' not in src, 'old _build_prompt still present'; assert 'approved [option name]' not in src, 'old approval prompt still present'; assert 'classify_complexity' in src; assert 'generate_diagram' in src; assert 'generate_viewer_url' in src; assert 'is_complex' in src; print('OK')"</automated>
  </verify>
  <done>
    backend/services/architecture_pipeline.py: no _parse_options, no _build_prompt, no "Reply 'approved [option name]'"; contains classify_complexity, generate_diagram, generate_viewer_url, is_complex, publish_architecture, and PipelineState status="complete". Python import succeeds with no errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → section parser | Untrusted free-text from freellmapi; _parse_sections must not raise on malformed output |
| issue_description → LLM prompt | User-controlled Jira text enters prompt; T-04-01 mitigated by never interpolating token values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | LLM prompt in architecture_pipeline | mitigate | T-04-01: prompt contains only issue_key/summary/description — no token values ever interpolated |
| T-13-02 | Denial of Service | Confluence publish failure blocking pipeline | mitigate | T-04-03: publish_architecture wrapped in try/except; pipeline continues without URL |
| T-13-03 | Information Disclosure | draft_content containing LLM output stored in DB | accept | draft_content is architecture prose, no credentials; DB access controlled by same layer as project tokens (T-03-07) |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — all dependencies already present in codebase |
</threat_model>

<verification>
- `python -c "import services.architecture_pipeline"` exits 0 with no ImportError
- `grep -c '_parse_options' backend/services/architecture_pipeline.py` returns 0
- `grep -c '_build_prompt' backend/services/architecture_pipeline.py` returns 0
- `grep -c 'approved \[option name\]' backend/services/architecture_pipeline.py` returns 0
- `grep -c 'classify_complexity' backend/services/architecture_pipeline.py` is greater than 0
- `grep -c 'generate_viewer_url' backend/services/architecture_pipeline.py` is greater than 0
</verification>

<success_criteria>
architecture_pipeline.py is fully rewritten: single-pass flow, complexity-aware branching, PipelineState created with status="running" then updated to status="complete", no multi-option code, no approval prompt, all required imports present. Python import succeeds.
</success_criteria>

<output>
Create `.planning/phases/13-pipeline-orchestration-integration/13-01-SUMMARY.md` when done.
</output>
