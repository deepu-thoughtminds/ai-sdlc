---
phase: 13-pipeline-orchestration-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - backend/tests/test_architecture_pipeline.py
  - backend/tests/test_webhook.py
autonomous: true
requirements:
  - ARCHGEN-01
  - ARCHGEN-02
  - ARCHGEN-03
  - ARCHINT-02

must_haves:
  truths:
    - "All existing tests in test_architecture_pipeline.py are replaced with tests matching the new pipeline API; test suite passes"
    - "complex path test: classify_complexity returns ('complex', ...) → generate_diagram called, publish_architecture called with is_complex=True, comment contains 'Multi-component feature — diagram included'"
    - "simple path test: classify_complexity returns ('small', ...) → generate_diagram NOT called, publish_architecture called with is_complex=False, comment contains 'Simple change — text architecture'"
    - "PipelineState.draft_content after pipeline run is a human-readable string — does not start with '{' and is not parseable as JSON"
    - "idempotency test in test_webhook.py: second @jarvis architecture webhook with active row returns action=ignored, reason=duplicate_pipeline, asyncio.create_task NOT called"
    - "All pre-existing tests in test_webhook.py (tests 1-10) continue to pass unchanged"
  artifacts:
    - path: "backend/tests/test_architecture_pipeline.py"
      provides: "End-to-end tests for new single-pass pipeline"
    - path: "backend/tests/test_webhook.py"
      provides: "Idempotency test for duplicate architecture webhook"
  key_links:
    - from: "test_run_complex_path"
      to: "services.architecture_pipeline.classify_complexity mock"
      via: "patch('services.architecture_pipeline.classify_complexity')"
    - from: "test_run_simple_path"
      to: "services.architecture_pipeline.generate_diagram assertion"
      via: "mock_generate_diagram.assert_not_called()"
---

<objective>
Replace the old test_architecture_pipeline.py tests (which tested multi-option flow) with tests for the new single-pass complexity-aware pipeline. Add an idempotency test to test_webhook.py.

Purpose: All five success criteria for Phase 13 require tests to pass. The old tests assert "Option" in result and status="awaiting_approval" — both are now incorrect and must be replaced.

Output: test_architecture_pipeline.py with 5 new tests (complex path, simple path, Confluence failure graceful, draft_content is human-readable string, LLM called with 'architecture' stage); test_webhook.py with 1 new test for idempotency guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/tests/test_architecture_pipeline.py
@backend/tests/test_webhook.py
@backend/services/architecture_pipeline.py
@backend/models/pipeline_state.py
@.planning/phases/13-pipeline-orchestration-integration/13-01-SUMMARY.md
@.planning/phases/13-pipeline-orchestration-integration/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace test_architecture_pipeline.py for new pipeline</name>
  <files>backend/tests/test_architecture_pipeline.py</files>
  <behavior>
    - test_run_complex_path: mock classify_complexity returning ("complex", "touches 3 services"); mock generate_diagram returning "<mxGraphModel/>"; mock generate_viewer_url returning "https://diagrams.net/view"; mock publish_architecture returning "https://conf.example.com/pages/1"; run() returns string containing "Multi-component feature — diagram included"; generate_diagram called once; publish_architecture called with is_complex=True
    - test_run_simple_path: mock classify_complexity returning ("small", "single service"); run() returns string containing "Simple change — text architecture"; generate_diagram NOT called; publish_architecture called with is_complex=False
    - test_run_draft_content_is_human_readable: after run(), PipelineState.draft_content is a str that is not valid JSON (json.loads raises) and does not start with "{"
    - test_run_creates_pipeline_state_complete: PipelineState row has stage="architecture" and status="complete" (not "awaiting_approval") after run() completes
    - test_run_graceful_on_confluence_failure: publish_architecture raises Exception; run() still returns non-empty string; "https://conf" not in result
    - test_run_calls_llm_with_architecture_stage: route_request called with first arg "architecture"; issue_summary in the prompt
  </behavior>
  <action>
    Completely replace backend/tests/test_architecture_pipeline.py. Preserve the same file header conventions (env var setup BEFORE imports, StaticPool in-memory DB, reset_tables autouse fixture, _make_mock_project helper, _make_db helper).

    Update _make_stub_llm_response() to return content matching the new section-based format that the new pipeline parses. For complex path, the stub content should include the six sections: "## Summary\nThis is the summary.\n## Approach\nApproach text.\n## Component Breakdown\nService A, Service B\n## Integration Points\nAPI gateway\n## Key Decisions\nUse microservices\n## Risks\nHigh coupling". For simple path, four sections: "## Summary\nSimple summary.\n## Approach\nSimple approach.\n## Key Decisions\nMinimal change\n## Risks\nLow risk".

    In each test that patches, use the correct patch targets for the new pipeline:
    - "services.architecture_pipeline.classify_complexity" — patch as a regular MagicMock (sync function) returning ("complex", "rationale") or ("small", "rationale")
    - "services.architecture_pipeline.route_request" — patch as MagicMock returning _make_stub_llm_response(content)
    - "services.architecture_pipeline.generate_diagram" — patch as MagicMock returning "<mxGraphModel/>"
    - "services.architecture_pipeline.generate_viewer_url" — patch as MagicMock returning "https://diagrams.net/view"
    - "services.architecture_pipeline.publish_architecture" — patch as AsyncMock returning "https://conf.example.com/wiki/spaces/PROJ/pages/1"

    For test_run_draft_content_is_human_readable: run the pipeline with the real in-memory DB (insert a real Project row for FK), then query PipelineState by ticket_key and stage="architecture", assert row.draft_content is not None, assert not row.draft_content.strip().startswith("{"), assert json.loads raises (import json in the test).

    For test_run_creates_pipeline_state_complete: query PipelineState after run(), assert rows[0].status == "complete".

    For test_run_graceful_on_confluence_failure: publish_architecture side_effect=Exception("network error"); assert result is str with len > 0; assert "https://conf" not in result.

    All six tests use @pytest.mark.asyncio and follow the try/finally db.close() pattern from the original file.

    Remove all references to "awaiting_approval", "Option", "options", "_parse_options". Do not import or reference any deleted functions.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -m pytest tests/test_architecture_pipeline.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 tests in test_architecture_pipeline.py pass. No "awaiting_approval" or "Option" assertions remain. draft_content human-readable assertion passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add idempotency test to test_webhook.py</name>
  <files>backend/tests/test_webhook.py</files>
  <action>
    Append a new test function to backend/tests/test_webhook.py. Do NOT modify or remove any of the existing 10 tests (tests 1-10). Update the module docstring count from 10 to 11.

    Add the following test after the last existing test:

    test_architecture_duplicate_webhook_returns_ignored (Test 11):
    - Create a PROJ project in the test DB.
    - Insert a PipelineState row for PROJ-1, stage="architecture", status="running" directly into the DB (simulating an in-progress pipeline).
    - POST /webhook/jira-comment with ARCHITECTURE_PAYLOAD (already defined in the file: "@jarvis architecture" for PROJ-1).
    - Patch asyncio.create_task to detect if it is called.
    - Assert: response status 200, body["action"] == "ignored", body["reason"] == "duplicate_pipeline".
    - Assert: asyncio.create_task was NOT called (mock_create_task.call_count == 0).

    Use the existing ARCHITECTURE_PAYLOAD dict already defined in the file. Reuse _create_project() and TestingSession. Insert the PipelineState row using the project.id FK from the created project.

    The test docstring must explain the idempotency guard: "Second @jarvis architecture webhook with an existing non-failed PipelineState row returns action=ignored without scheduling a new pipeline task."
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -m pytest tests/test_webhook.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 11 tests in test_webhook.py pass. Test 11 asserts action=ignored, reason=duplicate_pipeline, and create_task not called.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression — all backend tests pass</name>
  <files></files>
  <action>
    Run the full backend test suite. If any test fails that is NOT in test_architecture_pipeline.py or test_webhook.py (i.e., an unexpected regression), investigate and fix the root cause in the source file that changed (not by deleting the test).

    Common regression risks after this phase:
    - test_approval_detector.py: the architecture approval path tests (if any exist) now must match the removed code. If existing tests assert architecture approval behavior that no longer exists, update those tests to reflect the new describe-only scope — but only remove assertions for code that was explicitly deleted; preserve all describe-flow tests.
    - Any test that imports from architecture_pipeline and asserts on _parse_options or _build_prompt — remove those assertions.

    Run: `python -m pytest tests/ -v --tb=short 2>&1 | tail -40`

    If all pass: done. If failures in test_approval_detector.py for architecture-related assertions: update those specific test cases to not reference the removed architecture approval flow.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -m pytest tests/ -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR|passed|failed|error)" | tail -20</automated>
  </verify>
  <done>
    Full test suite: 0 FAILED, 0 ERROR. All passing tests remain green. No tests skipped that were previously passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → production code | Mocks must match actual function signatures to avoid false-positive tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | Test stubs returning incorrect types causing false-positive assertions | mitigate | Stub LLM response content uses real section-header format that the new _parse_sections() helper can parse |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — test files only; existing pytest, pytest-asyncio already installed |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_architecture_pipeline.py -v` → all 6 tests PASSED
- `python -m pytest backend/tests/test_webhook.py -v` → all 11 tests PASSED
- `python -m pytest backend/tests/ -q` → 0 failed, 0 errors
- `grep -c 'awaiting_approval' backend/tests/test_architecture_pipeline.py` returns 0
- `grep -c 'duplicate_pipeline' backend/tests/test_webhook.py` returns >= 1
</verification>

<success_criteria>
All backend tests pass. test_architecture_pipeline.py: 6 tests covering complex path, simple path, draft_content human-readable, status=complete, Confluence graceful failure, LLM stage='architecture'. test_webhook.py: 11 tests including idempotency guard test. No test asserts old "awaiting_approval" status or "Option" content for architecture pipeline.
</success_criteria>

<output>
Create `.planning/phases/13-pipeline-orchestration-integration/13-03-SUMMARY.md` when done.
</output>
