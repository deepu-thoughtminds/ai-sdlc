---
context: phase
phase: 17-pr-merge-pipeline
task: 1
total_tasks: 6
status: not_started
last_updated: 2026-06-21T02:36:39.098Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale context-exhaustion warning on fresh subagent | The first `gsd-planner` subagent spawn for this phase aborted immediately, citing a "75% context used" warning and recommending `/gsd-pause-work` — but it was a brand-new subagent with a full context budget. The warning was a carryover artifact from the parent session, not the subagent's actual state. | advisory | If a freshly-spawned subagent claims context exhaustion before doing any real work, just retry the spawn once with an explicit note in the prompt that it has a full budget — this resolved it cleanly on the second attempt. |

<current_state>
Phase 16 (dev-pipeline-integration) is fully complete (6 tasks across 2 plans, all tests passing, STATE.md/ROADMAP.md updated, committed).

Phase 17 (pr-merge-pipeline) has been PLANNED but NOT EXECUTED. `gsd-planner` produced:
- `.planning/phases/17-pr-merge-pipeline/17-01-PLAN.md` (wave 1, 3 tasks, no deps)
- `.planning/phases/17-pr-merge-pipeline/17-02-PLAN.md` (wave 2, 3 tasks, depends_on 17-01)

Both committed in `fff9d86` (force-added since `.planning/` is gitignored). Zero implementation work has started — no files in either plan's `files_modified` list have been touched yet.
</current_state>

<completed_work>

Completed Tasks:
- Phase 16 Plan 16-01: dev_pipeline.py + test_dev_pipeline.py — Done, committed `3db69cd`
- Phase 16 Plan 16-02: webhook.py start_coding wiring + tests — Done, committed `8ad62c4`
- Phase 16 SUMMARY.md + STATE/ROADMAP update — Done, committed `759b764` / `f0c00ee`
- Phase 17 planning (both PLAN.md files) — Done, committed `fff9d86`
</completed_work>

<remaining_work>

- 17-01 Task 1: Implement `find_and_merge_pr()` in `backend/services/pr_creator.py` — finds open PR by branch `jarvis/issue-{key}` or title match via GitHub API, merges to main.
- 17-01 Task 2: Add Jira status-transition support — likely a new `hermes_client` wrapper plus `hermes/mcp_client.py` / `hermes/server.py` additions (`transition_issue` or similar) — check 17-01-PLAN.md for exact function/endpoint names before starting.
- 17-01 Task 3: per plan frontmatter, 17-01 has 3 tasks total — read the full PLAN.md for the third task's exact scope before executing.
- 17-02 Task 1: `merge_pipeline.py` orchestrator (idempotency guard pattern identical to `dev_pipeline.py`/`architecture_pipeline.py` — `PipelineState(status="running")` committed before `asyncio.create_task`, CR-02 session isolation, WR-01 post-comment-before-complete, WR-03 best-effort failure notification).
- 17-02 Task 2: Replace the `merge_pr` stub in `backend/routers/webhook.py` (currently returns `routed_to: "pending_phase_17"`) with real wiring mirroring the `start_coding`/`architecture` branches.
- 17-02 Task 3: per plan frontmatter, 17-02 has 3 tasks total — read the full PLAN.md for the third task's exact scope before executing.
</remaining_work>

<decisions_made>

- Split Phase 17 into 2 plans/waves (service layer first, orchestrator+webhook wiring second) — same shape as Phase 16's split, so the orchestrator plan can depend on a finished, tested service function.
- User explicitly declined `/gsd-secure-phase 16` (post-hoc security audit command) — do NOT run or re-offer it for Phase 16. This is unrelated to the plan-time `<threat_model>` blocks already embedded in both 17-01/17-02 PLAN.md files (required by `workflow.security_enforcement: true` in `.planning/config.json`, a separate, lighter-weight mechanism than the full audit).
- `.planning/` is gitignored project-wide but PLAN.md/SUMMARY.md/STATE.md/ROADMAP.md are force-added (`git add -f`) by convention every time — continue this pattern for any new planning docs.
</decisions_made>

<blockers>
None. Planning is complete and clean; ready for execution.
</blockers>

## Required Reading (in order)
1. `.planning/phases/17-pr-merge-pipeline/17-01-PLAN.md` — full task breakdown, threat model, must_haves for wave 1
2. `.planning/phases/17-pr-merge-pipeline/17-02-PLAN.md` — full task breakdown, threat model, must_haves for wave 2
3. `backend/services/dev_pipeline.py` and `backend/services/architecture_pipeline.py` — the orchestrator pattern `merge_pipeline.py` must follow exactly (idempotency guard, CR-02, WR-01, WR-03, AGENT_COMMENT_PREFIX/AGENT_BODY_MARKER constants)
4. `backend/routers/webhook.py` — locate the `merge_pr` stub (`routed_to: "pending_phase_17"`) and the `start_coding`/`architecture` branches directly above it that 17-02 Task 2 must mirror

## Critical Anti-Patterns (do NOT repeat these)
- Do not assume a freshly-spawned subagent's context-exhaustion claim is real without checking — it can be a stale carryover warning. Retry once with an explicit "you have a full budget" note if this happens again.

## Infrastructure State
- No running servers/watchers. No pending human actions (no new API keys/MCP setup needed for Phase 17 — github_token/jira_token already exist on Project model from Phase 15/16).

<context>
Both Phase 17 plans are written and committed. There are no open planning questions. The straightforward next step is to run `/gsd-execute-phase 17`, which will spawn `gsd-executor` subagents wave-by-wave (Wave 1 = 17-01, Wave 2 = 17-02 once 17-01's tasks are done and its tests pass). Follow the same TDD/atomic-commit-per-task discipline used in Phase 16. Watch for the same kind of pre-existing unrelated test failures (e.g. test_assign_pipeline.py) — confirm via `git stash` if any unexpected failures appear, don't assume they're caused by this phase's work without checking.
</context>

<next_action>
Start with: Run `/gsd-execute-phase 17` (no flags needed — full phase execution, both waves)
</next_action>
