---
plan_id: "18-02"
phase: 18
plan: 2
type: execute
wave: 2
title: "Trigger Wiring + Tests"
depends_on: ["18-01"]
requirements: [SCAN-01, SCAN-02, SCAN-03]
files_modified:
  - backend/routers/projects.py
  - backend/tests/test_codebase_scan_service.py
autonomous: true
must_haves:
  truths:
    - "create_project is converted to async def — one-line signature change, no behavioural difference for callers"
    - "PipelineState(stage='codebase_scan', status='running') is committed to DB BEFORE asyncio.create_task() is called"
    - "ticket_key uses sentinel '__onboarding__' (no Jira ticket at project creation time)"
    - "Background task opens its own SessionLocal() — never reuses the request-scoped db session (per CR-02 convention)"
    - "Scan only triggered when payload.github_repo is non-empty"
    - "All unit tests pass: file select priority, stack detection, tree builder, markdown sections, PUT idempotency sha, github error handling"
  artifacts:
    - path: "backend/routers/projects.py"
      provides: "async create_project with asyncio.create_task scan trigger"
      contains: "asyncio.create_task"
    - path: "backend/tests/test_codebase_scan_service.py"
      provides: "Unit tests for codebase_scan_service helpers and run() coroutine"
      exports: ["test_select_key_files_priority", "test_detect_tech_stack_python_fastapi", "test_build_ascii_tree", "test_run_skips_when_no_github_repo", "test_run_commits_snapshot", "test_run_handles_file_404"]
  key_links:
    - from: "backend/routers/projects.py create_project"
      to: "backend/services/codebase_scan_service.run"
      via: "asyncio.create_task(_run_scan_background())"
      pattern: "create_task"
    - from: "_run_scan_background"
      to: "SessionLocal()"
      via: "fresh session per background task"
      pattern: "SessionLocal"
---

<objective>
Wire the codebase scan trigger into projects.py (convert create_project to async, commit PipelineState row, schedule asyncio.create_task) and deliver a complete unit-test suite for codebase_scan_service.py using respx to mock all GitHub API calls.

Purpose: SCAN-01 (automatic scan on project onboarding), plus regression coverage for SCAN-02 and SCAN-03 helpers.
Output: modified backend/routers/projects.py, new backend/tests/test_codebase_scan_service.py
</objective>

<execution_context>
@/home/deepu/.claude/gsd-core/workflows/execute-plan.md
@/home/deepu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@backend/routers/projects.py
@backend/routers/webhook.py
@backend/models/pipeline_state.py
@backend/database.py
@backend/tests/test_graphify_service.py
@.planning/phases/18-codebase-scan-service/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire scan trigger in projects.py</name>
  <files>backend/routers/projects.py</files>
  <action>
    Modify backend/routers/projects.py to trigger the codebase scan as an asyncio background task whenever a new project is created with a non-empty github_repo. Follow the webhook.py convention exactly (CR-02: no request-scoped session in background).

    Required imports to add at the top of projects.py:
      import asyncio
      from database import SessionLocal
      from models.pipeline_state import PipelineState
      from services import codebase_scan_service
      from services.crypto import decrypt_credential

    (decrypt_credential is already imported — confirm before adding duplicate.)

    Change the create_project function signature from sync to async:
      BEFORE: def create_project(payload: ProjectCreate, db: Session = Depends(get_db)) -> ProjectPublic:
      AFTER:  async def create_project(payload: ProjectCreate, db: Session = Depends(get_db)) -> ProjectPublic:
    FastAPI handles async route handlers natively — no other change needed for the HTTP layer.

    After the existing "db.refresh(project)" and "logger.info" lines, add the scan trigger block. Only trigger if payload.github_repo is non-empty (github_repo may be an empty string for projects without a repo):

        if payload.github_repo:
            # SCAN-01: Trigger codebase scan as background task on project onboarding.
            # PipelineState committed BEFORE asyncio.create_task() so duplicate-guard
            # (Phase 19) can detect an in-flight scan. ticket_key "__onboarding__" is
            # the sentinel value used when no Jira ticket exists at onboarding time.
            scan_state = PipelineState(
                project_id=project.id,
                ticket_key="__onboarding__",
                stage="codebase_scan",
                status="running",
            )
            db.add(scan_state)
            db.commit()

            project_id = project.id

            async def _run_scan_background() -> None:
                # CR-02: Open a fresh session — never reuse the request-scoped db session.
                bg_db = SessionLocal()
                try:
                    bg_project = bg_db.query(Project).filter(Project.id == project_id).first()
                    if bg_project is None:
                        logger.warning(
                            "Project id=%s not found in scan background task — aborting",
                            project_id,
                        )
                        return
                    github_token = decrypt_credential(bg_project.github_token)  # T-18-01: not logged
                    github_repo = decrypt_credential(bg_project.github_repo)
                    try:
                        await codebase_scan_service.run(github_repo, github_token, project_id, bg_db)
                    except Exception as exc:
                        logger.warning(
                            "Codebase scan failed for project id=%s: %s", project_id, exc
                        )
                        # Mark PipelineState as error so UI can surface the failure
                        bg_db.query(PipelineState).filter(
                            PipelineState.project_id == project_id,
                            PipelineState.stage == "codebase_scan",
                            PipelineState.status == "running",
                        ).update({"status": "error"})
                        bg_db.commit()
                finally:
                    bg_db.close()

            asyncio.create_task(_run_scan_background())
            logger.info("Codebase scan scheduled project id=%d", project.id)  # T-18-01: no token

    The "return _project_to_public(project)" line at the end stays unchanged and still returns immediately — the scan runs in the background after the HTTP 201 response is already sent.

    Also add the Project import if not already present:
      from models.project import Project, ProjectCreate, ProjectListItem, ProjectPublic
    (Project is already imported — just confirm it includes Project ORM class, not only the Pydantic schemas.)
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -c "from routers.projects import create_project; import inspect; assert inspect.iscoroutinefunction(create_project), 'create_project must be async'"</automated>
  </verify>
  <done>create_project is async; asyncio.create_task block is present and guarded by "if payload.github_repo"; PipelineState with stage="codebase_scan" and ticket_key="__onboarding__" is committed before create_task; _run_scan_background opens its own SessionLocal().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for codebase_scan_service</name>
  <files>backend/tests/test_codebase_scan_service.py</files>
  <behavior>
    - test_select_key_files_priority: paths including node_modules, .git, README.md, pyproject.toml, main.py, frontend/src/page.tsx → result starts with README.md, then pyproject.toml (manifest before entry points), node_modules excluded
    - test_select_key_files_max_budget: 30 eligible paths → result has exactly MAX_FILES (25) entries
    - test_select_key_files_skips_extensions: paths with .lock, .min.js, .pyc, .png → all excluded from result
    - test_detect_tech_stack_python_fastapi: paths=["pyproject.toml"], manifest_contents={"pyproject.toml": "fastapi==0.115"} → result contains "Python (pyproject.toml)" and "FastAPI (pyproject.toml)"
    - test_detect_tech_stack_docker: paths=["Dockerfile", "docker-compose.yml"] → result contains "Docker (Dockerfile)"
    - test_detect_tech_stack_nextjs: paths=["package.json"], manifest_contents={"package.json": '{"next": "13.0"}'} → result contains "Next.js (package.json)"
    - test_build_ascii_tree_depth: paths with depth > 3 → tree string shows collapsed "{dir}/..." at depth limit
    - test_build_ascii_tree_skips_skip_dirs: paths including "node_modules/pkg/index.js" → node_modules not in tree output
    - test_build_markdown_sections: output contains "## Tech Stack", "## Directory Structure", "## Key Files", "## Module Summary"
    - test_run_skips_when_no_github_repo: run("", "token", 1, mock_db) → returns without making any HTTP call
    - test_run_skips_when_invalid_slug: run("not-a/valid/slug/extra", "token", 1, mock_db) → returns without HTTP call
    - test_run_commits_snapshot: mock Trees API + 2 file reads + GET .hermes/codebase.md (404) + PUT → all called; PUT body has "content" key and "message" = "chore: update codebase snapshot [jarvis-scan]"
    - test_run_idempotent_put_includes_sha: mock .hermes/codebase.md GET returns 200 with sha="abc123" → PUT body includes "sha": "abc123"
    - test_run_handles_file_404: one file in selected list returns 404 → run completes without exception; other files still read
    - test_run_handles_trees_truncated: trees response has truncated=true → run completes and still calls PUT
  </behavior>
  <action>
    Create backend/tests/test_codebase_scan_service.py. Use respx for httpx mocking (already in requirements.txt as respx==0.23.1). Use pytest-asyncio for async test functions (pytest-asyncio==0.24.0 already installed).

    File header: import asyncio, base64, os; import pytest, respx, httpx; from unittest.mock import MagicMock; from services.codebase_scan_service import run, _select_key_files, _build_ascii_tree, _detect_tech_stack, _build_markdown, MAX_FILES.

    Set at module level: os.environ.setdefault("GITHUB_API_BASE", "https://api.github.com").

    Helper _make_tree_response(paths, truncated=False): returns GitHub tree JSON dict with "tree": [{"path": p, "type": "blob"} for p in paths] and "truncated": truncated.

    Helper _encode_content(text): returns base64.b64encode(text.encode()).decode().

    Helper _make_db(): returns MagicMock() — tests that call run() need a db stub but the service only uses db for PipelineState updates (which are handled by the caller in projects.py in Phase 18 — the service itself does NOT update PipelineState, so the mock can be minimal).

    Note on run() signature: run(github_repo, github_token, project_id, db). In tests that verify "no HTTP call made", use respx.mock decorator and assert no routes were called.

    For async tests that call run() directly, decorate with @pytest.mark.asyncio and @respx.mock.

    For test_run_commits_snapshot: mock four endpoints:
      1. GET https://api.github.com/repos/org/repo → {"default_branch": "main"}
      2. GET https://api.github.com/repos/org/repo/git/trees/HEAD → _make_tree_response(["README.md", "backend/main.py"])
      3. GET https://api.github.com/repos/org/repo/contents/README.md → {"content": _encode_content("# My App"), "encoding": "base64"}
      4. GET https://api.github.com/repos/org/repo/contents/backend/main.py → {"content": _encode_content("# main"), "encoding": "base64"}
      5. GET https://api.github.com/repos/org/repo/contents/.hermes/codebase.md → 404 (file does not yet exist)
      6. PUT https://api.github.com/repos/org/repo/contents/.hermes/codebase.md → 201 with body {"content": {"sha": "newsha"}}
    Assert: respx route for PUT was called exactly once; the PUT request JSON body has "message" == "chore: update codebase snapshot [jarvis-scan]"; "sha" key is NOT present in PUT body (because GET returned 404).

    For test_run_idempotent_put_includes_sha: same as above but GET .hermes/codebase.md returns 200 with {"sha": "abc123", "content": _encode_content("old content")}; assert PUT body includes "sha": "abc123".

    For test_run_handles_file_404: tree has ["README.md", "missing.py"]; README.md contents mock returns 200; missing.py returns 404; PUT still called once — no exception propagates.

    Unit tests for helpers (synchronous, no respx needed):
      test_select_key_files_priority, test_select_key_files_max_budget, test_select_key_files_skips_extensions: call _select_key_files directly with constructed path lists.
      test_detect_tech_stack_*: call _detect_tech_stack directly.
      test_build_ascii_tree_*: call _build_ascii_tree directly.
      test_build_markdown_sections: call _build_markdown with minimal stub args; assert section headers present in returned string.

    Do NOT write tests that verify logging output — test observable return values and HTTP call patterns only.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc-jira/backend && python -m pytest tests/test_codebase_scan_service.py -x -q</automated>
  </verify>
  <done>All tests in test_codebase_scan_service.py pass. Test count is at least 14. No test imports subprocess or os.system. respx mocks verify that github_token is not present in any logged output (checked by confirming no caplog fixture captures token-shaped strings).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create_project HTTP handler → asyncio.create_task | Decrypted github_token passed only to codebase_scan_service.run(); never logged |
| _run_scan_background → SessionLocal | Fresh DB session opened per background task; request-scoped session never reused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Information Disclosure | _run_scan_background — decrypt_credential usage | mitigate | Decrypted github_token assigned to local var; passed directly to codebase_scan_service.run(); never assigned to logger args (T-18-01 convention enforced here too) |
| T-18-06 | Denial of Service | asyncio.create_task without await | accept | FastAPI event loop keeps background tasks alive; consistent with webhook.py pattern proven in Phases 16-17; task failure updates PipelineState to "error" without crashing the web process |
| T-18-07 | Tampering | PipelineState ticket_key sentinel "__onboarding__" | accept | Sentinel is a known constant, not user input; no SQL injection risk via ORM parameterized query |
| T-18-SC | Tampering | No new package installs | accept | No new pip packages; asyncio and httpx already present |
</threat_model>

<verification>
1. `cd backend && python -c "from routers.projects import create_project; import inspect; assert inspect.iscoroutinefunction(create_project)"` passes
2. `grep -c "asyncio.create_task" backend/routers/projects.py` returns 1
3. `grep -c "ticket_key.*__onboarding__\|__onboarding__.*ticket_key" backend/routers/projects.py` returns at least 1
4. `cd backend && python -m pytest tests/test_codebase_scan_service.py -x -q` — all tests pass
5. `grep -v "^#" backend/routers/projects.py | grep -c "github_token"` — only assignment lines (decrypt + pass to service), zero logger.* references
</verification>

<success_criteria>
- create_project in projects.py is async def
- Codebase scan is automatically triggered when payload.github_repo is non-empty
- PipelineState row (stage="codebase_scan", status="running", ticket_key="__onboarding__") committed before asyncio.create_task
- Background helper uses fresh SessionLocal(), closes it in finally block
- test_codebase_scan_service.py has >= 14 tests, all pass
- Phase 18 success criteria 1-4 from ROADMAP.md all satisfied
</success_criteria>

<output>
Create .planning/phases/18-codebase-scan-service/18-02-SUMMARY.md when done.
</output>
