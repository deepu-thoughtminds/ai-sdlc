---
phase: 18-codebase-scan-service
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_projects.py
autonomous: true
gap_closure: true
requirements: [SCAN-01]
must_haves:
  truths:
    - "POSTing /api/projects with a non-empty github_repo creates a PipelineState row with stage='codebase_scan', status='running', ticket_key='__onboarding__'"
    - "codebase_scan_service.run is invoked (directly or via the asyncio.create_task background wrapper) when a project is created with a non-empty github_repo"
  artifacts:
    - path: "backend/tests/test_projects.py"
      provides: "Automated regression coverage for the SCAN-01 background scan trigger wiring in routers/projects.py"
      contains: "test_create_project_with_github_repo_commits_pipeline_state"
  key_links:
    - from: "backend/tests/test_projects.py"
      to: "backend/routers/projects.py create_project"
      via: "TestClient.post('/api/projects', ...) then direct PipelineState query"
      pattern: "stage.*codebase_scan"
    - from: "backend/tests/test_projects.py"
      to: "services.codebase_scan_service.run"
      via: "monkeypatch.setattr on services.codebase_scan_service.run"
      pattern: "monkeypatch.setattr"
---

<objective>
Close the single remaining Phase 18 verification gap: add automated test coverage in `backend/tests/test_projects.py` proving that POSTing to `/api/projects` with a non-empty `github_repo` (1) commits a `PipelineState(stage='codebase_scan', status='running', ticket_key='__onboarding__')` row, and (2) schedules the background scan task that calls `codebase_scan_service.run`.

This is a TEST-ONLY gap closure plan. No production code in `backend/routers/projects.py` or `backend/services/codebase_scan_service.py` is modified — per 18-VERIFICATION.md, the wiring is already correct by static read; only regression-test coverage is missing.

Purpose: SCAN-01's automatic-scan-trigger behavior must be regression-tested so a future refactor of `create_project` cannot silently break the trigger without a failing test (the gap identified in 18-VERIFICATION.md).
Output: two new test functions added to `backend/tests/test_projects.py`.
</objective>

<execution_context>
@/home/deepu/.claude/gsd-core/workflows/execute-plan.md
@/home/deepu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-codebase-scan-service/18-VERIFICATION.md
@backend/routers/projects.py
@backend/models/pipeline_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PipelineState-commit and scan-trigger regression tests to test_projects.py</name>
  <files>backend/tests/test_projects.py</files>
  <read_first>
    backend/tests/test_projects.py
    backend/routers/projects.py
    backend/models/pipeline_state.py
  </read_first>
  <action>
    Add two new test functions to backend/tests/test_projects.py, placed after the existing test_get_project_includes_github_repo function (end of file). Both tests must use the module's existing `client` (TestClient), `TestingSession`, and `_unique_payload()` fixture helpers — do not introduce a new TestClient or engine.

    Test 1 — test_create_project_with_github_repo_commits_pipeline_state:
    - Import PipelineState at top of function body via `from models.pipeline_state import PipelineState` (local import, matching the existing local-import style used for Project in other tests in this file).
    - Build payload via `_unique_payload()` (already sets `github_repo": "acme/my-app"`).
    - POST to "/api/projects" with that payload via `client.post`; assert status_code == 201.
    - Extract `project_id = response.json()["id"]`.
    - Open a `db = TestingSession()` session (same pattern as test_create_project_persists_encrypted), query `db.query(PipelineState).filter(PipelineState.project_id == project_id, PipelineState.stage == "codebase_scan").first()`.
    - Assert the row is not None.
    - Assert `row.status == "running"`.
    - Assert `row.ticket_key == "__onboarding__"`.
    - Close the db session in a finally block (mirror existing pattern).
    - Note: because `asyncio.create_task` for `_run_scan_background` fires only after the `db.commit()` for `scan_state` (line ~99 of projects.py, which happens before `asyncio.create_task` on line 132), the PipelineState row is guaranteed to exist by the time the HTTP response returns — no need to await or sleep for the background task itself in this test.

    Test 2 — test_create_project_with_github_repo_schedules_scan_run (uses monkeypatch fixture, a pytest builtin — add `monkeypatch` as a parameter to the test function signature):
    - At the top of the test, define an async stub: `async def _fake_run(github_repo, github_token, project_id, db): _fake_run.called = True; _fake_run.call_args = (github_repo, github_token, project_id)` and initialize `_fake_run.called = False` before patching.
    - Apply `monkeypatch.setattr("routers.projects.codebase_scan_service.run", _fake_run)` — patch the `run` attribute via the `codebase_scan_service` module object as imported inside `routers.projects` (the router does `from services import codebase_scan_service` then calls `codebase_scan_service.run(...)`, so patching the attribute on that imported module object, reached through `routers.projects.codebase_scan_service`, intercepts the call without needing to patch `services.codebase_scan_service` directly).
    - Build payload via `_unique_payload()`.
    - POST to "/api/projects" via `client.post`; assert status_code == 201.
    - Because `asyncio.create_task` schedules `_run_scan_background` as a fire-and-forget coroutine on the running event loop and TestClient (httpx-based, using anyio's event loop under the hood in this FastAPI/Starlette version) does not guarantee the task completes before `client.post` returns control, the test must explicitly give the event loop a chance to run the scheduled task: call `import asyncio` at module scope if not already imported (check existing imports first — file does not currently import asyncio, add it to the top-level import block alongside `import os`), then in the test, after the POST, run `asyncio.run(asyncio.sleep(0.05))` is NOT viable inside a sync test calling into TestClient's already-closed loop — instead use the simpler and deterministic approach: replace `_fake_run` with a synchronous-completing coroutine and rely on TestClient's internal behavior of running the ASGI app via anyio's `from_thread`/`run_sync` machinery, which drains pending tasks created during the request before returning. Since FastAPI's TestClient (via httpx ASGITransport) executes the request inside a single `anyio.run()` call that does NOT wait for fire-and-forget tasks created via `asyncio.create_task` after the response is built, assert readiness defensively: after the POST, poll with a short bounded loop — `for _ in range(20): import time; time.sleep(0.01); if _fake_run.called: break` — then assert `_fake_run.called is True`.
    - Assert `_fake_run.call_args[2] == response.json()["id"]` (project_id passed matches the created project).
  </action>
  <acceptance_criteria>
    - `backend/tests/test_projects.py` contains exactly two new test functions: `test_create_project_with_github_repo_commits_pipeline_state` and `test_create_project_with_github_repo_schedules_scan_run`.
    - Running `python3 -m pytest tests/test_projects.py -q` from the `backend/` directory shows all tests passing, including the two new ones, with zero failures and zero errors.
    - `python3 -m pytest tests/test_projects.py -k "pipeline_state or schedules_scan_run" -v` shows both new tests passing individually.
    - No modifications were made to `backend/routers/projects.py` or `backend/services/codebase_scan_service.py` (verify via `git diff --stat backend/routers/projects.py backend/services/codebase_scan_service.py` showing no output).
    - Full backend suite has no new regressions versus the pre-existing 5 documented failures: `python3 -m pytest tests/ -q` shows pass count increased by exactly 2 versus the prior run (237 -> 239 passed), with the same 5 pre-existing failures (test_assign_pipeline.py x4, test_repo_clone.py x1) and 3 skipped, unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_projects.py -q</automated>
  </verify>
  <done>Two new tests exist in test_projects.py, both pass, and they assert (1) a PipelineState row with stage='codebase_scan', status='running', ticket_key='__onboarding__' is committed on project creation with non-empty github_repo, and (2) codebase_scan_service.run is invoked via the asyncio.create_task background trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-process -> in-memory SQLite | Test-only boundary; no production trust boundary crossed by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-18-03-01 | Tampering | test fixtures (monkeypatch target string) | accept | Test-only code; incorrect monkeypatch target only causes test failure (fail-safe), no production risk |

No new packages installed; no production code touched. Package Legitimacy Gate not applicable (no install tasks).
</threat_model>

<verification>
Run `cd backend && python3 -m pytest tests/test_projects.py -v` and confirm all tests pass, including the two new gap-closure tests. Run `cd backend && python3 -m pytest tests/ -q` and confirm total pass count increases by exactly 2 with no new failures relative to the 18-VERIFICATION.md baseline (237 passed -> 239 passed, same 5 pre-existing failures, 3 skipped).
</verification>

<success_criteria>
- `test_create_project_with_github_repo_commits_pipeline_state` passes and asserts PipelineState(stage='codebase_scan', status='running', ticket_key='__onboarding__') exists after POST /api/projects with non-empty github_repo.
- `test_create_project_with_github_repo_schedules_scan_run` passes and asserts codebase_scan_service.run (patched) is invoked with the correct project_id when a project is created with non-empty github_repo.
- No production code files are modified (routers/projects.py and services/codebase_scan_service.py remain unchanged).
- The Phase 18 VERIFICATION.md gap ("Background scan trigger ... is exercised by an automated test" — partial) is fully closed: re-running phase verification should report this truth as fully VERIFIED.
</success_criteria>

<output>
Create `.planning/phases/18-codebase-scan-service/18-03-SUMMARY.md` when done
</output>
