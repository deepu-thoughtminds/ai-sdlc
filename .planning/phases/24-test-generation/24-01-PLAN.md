---
phase: 24-test-generation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/test_generator.py
  - backend/services/qa_pipeline.py
  - backend/tests/test_test_generator.py
  - backend/tests/test_qa_pipeline.py
autonomous: true
requirements: [TESTGEN-01, QAREP-01]

must_haves:
  truths:
    - "Agent generates a pytest unit test file grounded in the cloned repo's actual code plus .hermes/codebase.md context, via freellmapi, using the ### FILE: convention"
    - "The generated test file is written to the cloned workspace (with path-traversal protection) before any test execution happens"
    - "Generated unit tests execute in the QA sandbox via the existing test_executor.run_command/run_static_analysis machinery and produce a structured pass/fail/error result"
    - "After execution (pass or fail), a Jira comment is posted summarizing results per category: unit tests AND static analysis, at minimum"
    - "No manual configuration of the target repo is required — toolchain/test command is derived automatically from what test_executor.detect_toolchain already found, plus the generated test file path"
  artifacts:
    - path: "backend/services/test_generator.py"
      provides: "generate_unit_tests() — calls freellmapi (testgen stage) with repo file contents + codebase.md context; parses ### FILE: blocks into FileChange list reusing code_generator's convention"
      contains: "def generate_unit_tests"
    - path: "backend/services/qa_pipeline.py"
      provides: "run() extended to call generate_unit_tests, write the generated file into the cloned workspace with path-traversal guard, execute it via test_executor, and post a combined unit-test + static-analysis Jira comment"
      contains: "generate_unit_tests"
    - path: "backend/tests/test_test_generator.py"
      provides: "Unit tests for generate_unit_tests() and its FILE-block parsing/path-write logic"
      contains: "def test_"
    - path: "backend/tests/test_qa_pipeline.py"
      provides: "Extended tests covering the new unit-test-generation step in run()"
      contains: "def test_unit_test"
  key_links:
    - from: "backend/services/qa_pipeline.py"
      to: "backend/services/test_generator.py"
      via: "from services.test_generator import generate_unit_tests; call inside run()"
      pattern: "generate_unit_tests\\("
    - from: "backend/services/test_generator.py"
      to: "backend/services/llm_router.py"
      via: "route_request(\"testgen\", prompt) — testgen already in HEAVY_STAGES"
      pattern: "route_request\\(\"testgen\""
    - from: "backend/services/test_generator.py"
      to: "backend/services/codebase_snapshot_reader.py"
      via: "get_codebase_snapshot(github_repo, github_token) called by qa_pipeline.run() and its result passed into generate_unit_tests as codebase_context"
      pattern: "get_codebase_snapshot"
    - from: "backend/services/qa_pipeline.py"
      to: "backend/services/test_executor.py"
      via: "run_command(ToolchainCommand(...)) used to execute the generated pytest file inside the qa-sandbox container"
      pattern: "run_command\\("
---

<objective>
Extend the Phase 23 QA pipeline skeleton with LLM-driven pytest unit test generation, sandboxed execution of those generated tests, and a combined (unit test + static analysis) Jira comment summary.

Purpose: Close TESTGEN-01 (LLM generates grounded pytest tests) and QAREP-01 (per-category Jira summary) — the second of three QA-stage requirement clusters in the v1.8 milestone. Phase 23 built the architectural boundary (LLM generates files; orchestrator executes them) and validated it with static analysis only. This phase activates the LLM side of that boundary for the first time in the QA pipeline.
Output: `backend/services/test_generator.py` (new), `backend/services/qa_pipeline.py` (extended), and matching test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/code_generator.py
@backend/services/qa_pipeline.py
@backend/services/test_executor.py
@backend/services/llm_router.py
@backend/services/codebase_snapshot_reader.py
@backend/services/repo_clone.py
@backend/services/pr_creator.py
@backend/tests/test_qa_pipeline.py
@backend/tests/test_code_generator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backend/services/test_generator.py — LLM-grounded pytest generation</name>
  <files>backend/services/test_generator.py, backend/tests/test_test_generator.py</files>
  <read_first>
    backend/services/code_generator.py (the `### FILE:` parser `_parse_file_changes` and `FileChange` dataclass — reuse this exact regex/parsing convention, do not invent a new one; the generate_code_changes() prompt-construction style is the template to follow)
    backend/services/llm_router.py (route_request signature; HEAVY_STAGES already includes "testgen" at line 30 — no change needed there)
    backend/services/codebase_snapshot_reader.py (get_codebase_snapshot signature and None-on-failure contract — test_generator.py's caller (qa_pipeline.py) will supply codebase_context as a plain string, which may be empty/None)
    backend/tests/test_code_generator.py (existing test patterns for prompt-construction and FILE-block parsing — mirror this test structure for test_generator.py)
  </read_first>
  <behavior>
    - Test: `_parse_file_changes`-equivalent parser in test_generator.py (reuse code_generator's `_parse_file_changes` directly via import rather than re-implementing it — see action) returns a list of `FileChange` given a single `### FILE: tests/test_foo.py` block with a ```python fenced body.
    - Test: `generate_unit_tests()` calls `route_request("testgen", prompt)` exactly once and the prompt string contains the issue_key, the `relevant_file_contents` dict's file paths/content, and (when non-empty) the `codebase_context` string — assert via `mock_route_request.call_args` prompt content, not by re-deriving the prompt.
    - Test: when `route_request` returns `LLMResponse(content="[stub]")`, `generate_unit_tests()` returns an empty list (graceful degradation, same contract as `code_generator._parse_file_changes`).
    - Test: when `codebase_context` is `None` or `""`, the prompt still builds successfully (no `None` concatenated into the prompt string) and `generate_unit_tests()` does not raise.
    - Test: the prompt instructs the LLM to prefix generated test file paths with `tests/` and to use pytest conventions (assert via substring check on the constructed prompt, e.g. `"tests/"` and `"pytest"` appear in the prompt text passed to `route_request`).
  </behavior>
  <action>
    Create `backend/services/test_generator.py` mirroring `code_generator.py`'s structure exactly. Import `FileChange` and `_parse_file_changes` directly from `services.code_generator` (do not duplicate the regex parser — it is the established `### FILE:` convention referenced by TESTGEN-01). Define `generate_unit_tests(issue_key: str, issue_summary: str, issue_description: str, codebase_context: str | None, relevant_file_contents: dict[str, str]) -> list[FileChange]`. Build the prompt as a single string containing: issue_key, issue_summary, issue_description, a "Codebase context" section populated from `codebase_context` (use empty string when `codebase_context` is falsy — never concatenate `None`), a "Relevant file contents" section built the same way `code_generator.generate_code_changes` builds `file_blocks` (join `f"### {path}\n{content}"` per dict item), and an instruction block telling the LLM to respond using exactly the `### FILE: path/to/file.py` + fenced-code-block convention, to prefix every generated file path with `tests/`, to use pytest (`def test_...` functions, `assert` statements, no unittest.TestCase), and to generate tests only for behavior visible in the supplied file contents (no fabricated APIs). Call `route_request("testgen", prompt)` from `services.llm_router` and return `_parse_file_changes(route_result.content)`. Add a module docstring stating this implements TESTGEN-01 and listing the same T-06-01-style threat note as code_generator.py (no token/credential values interpolated into the prompt — only issue/codebase/file content). Write `backend/tests/test_test_generator.py` with the 5 behavior cases above, mocking `services.test_generator.route_request` (patch at the import site inside test_generator.py, i.e. `services.test_generator.route_request`, matching the `test_code_generator.py` mocking convention of patching `services.code_generator.route_request`).
  </action>
  <acceptance_criteria>
    - `backend/services/test_generator.py` exists and exports `generate_unit_tests`
    - `grep -n "from services.code_generator import" backend/services/test_generator.py` matches (reuses FileChange/_parse_file_changes, does not redefine them)
    - `grep -n 'route_request("testgen"' backend/services/test_generator.py` matches exactly once
    - `pytest backend/tests/test_test_generator.py -x -q` passes, all 5+ tests green
    - `grep -n "tests/" backend/services/test_generator.py` shows the prompt instructs `tests/`-prefixed paths
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_test_generator.py -x -q</automated>
  </verify>
  <done>generate_unit_tests() builds a grounded prompt (issue + codebase.md + relevant files), routes to freellmapi via the existing testgen heavy-stage path, and parses the response into FileChange objects using the established ### FILE: convention — with full test coverage including the stub/empty-context graceful-degradation paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire test generation + execution + combined reporting into qa_pipeline.py</name>
  <files>backend/services/qa_pipeline.py, backend/tests/test_qa_pipeline.py</files>
  <read_first>
    backend/services/qa_pipeline.py (current run() — Step 5 currently only calls run_static_analysis; this task inserts a new step between Step 4 (clone) and the existing Step 5, and reworks `_format_static_analysis_comment` into a combined formatter)
    backend/services/pr_creator.py (lines ~208-219 — the exact path-traversal guard pattern: `pathlib.Path(workspace_path).resolve()`, `(workspace_root / change.path).resolve()`, the `startswith(str(workspace_root) + "/")` check, `resolved.parent.mkdir(parents=True, exist_ok=True)`, `resolved.write_text(...)` — replicate this exact guard for writing the generated test file, do not invent a new one)
    backend/services/test_executor.py (ToolchainCommand/TestResult dataclasses and run_command signature — the generated pytest file is executed via a new ToolchainCommand built the same way detect_toolchain builds its docker run commands, i.e. `docker run --rm -v {workspace_path}:/workspace {image} pytest /workspace/{relative_test_path} -v`)
    backend/services/codebase_snapshot_reader.py (get_codebase_snapshot signature — async, returns str | None, never raises)
    backend/tests/test_qa_pipeline.py (existing fixtures: `_make_mock_project`, `_make_cloned_repo`, `_make_state_row` — reuse these; existing mocking convention via `patch("services.qa_pipeline.X")`)
  </read_first>
  <behavior>
    - Test: `run()` calls `get_codebase_snapshot(github_repo, github_token)` after cloning and before calling `generate_unit_tests`, passing its (possibly None) result through as `codebase_context`.
    - Test: `run()` calls `generate_unit_tests(...)` with `relevant_file_contents` built from reading files in the cloned workspace (mock this at `services.qa_pipeline.generate_unit_tests` so the file-reading detail is not asserted here — assert only that it is called once with `issue_key="PROJ-1"`).
    - Test: when `generate_unit_tests` returns one `FileChange(path="tests/test_generated.py", content="...")`, `run()` writes that file under the cloned workspace at the resolved path (assert via a `tmp_path`-based real cloned workspace fixture that the file exists with the expected content after `run()` completes — do not mock `pathlib.Path.write_text` for this test).
    - Test: when a `FileChange.path` resolves outside the workspace root (e.g. `"../../etc/passwd"`), `run()` does not write the file, does not raise an unhandled exception out of the pipeline, and the pipeline still completes (status set to `"failed"` for that ticket, failure comment posted) — mirroring the existing exception-handling try/except/finally structure already in `run()`.
    - Test: after the generated test file is written, `run()` executes it via `run_command` (mock `services.qa_pipeline.run_command`) and the resulting `TestResult` is included in the Jira comment alongside the static-analysis results — assert the posted comment contains both a unit-test-result line and at least one static-analysis line when both lists are non-empty.
    - Test: when `generate_unit_tests` returns an empty list (LLM stub/graceful degradation), `run()` skips the write+execute step entirely (no `run_command` call for a generated test file) and the Jira comment notes that no unit tests were generated, while static analysis results still appear.
    - Test: existing tests in `test_qa_pipeline.py` (clone/cleanup/qa_attempt/status-transition assertions) continue to pass with `services.qa_pipeline.generate_unit_tests` and `services.qa_pipeline.get_codebase_snapshot` mocked to return `[]` / `None` respectively, so the new step is a no-op for those pre-existing scenarios.
  </behavior>
  <action>
    In `backend/services/qa_pipeline.py`: add imports `from services.test_generator import generate_unit_tests`, `from services.codebase_snapshot_reader import get_codebase_snapshot`, `from services.test_executor import ToolchainCommand, run_command`, and `import pathlib`. Insert a new Step 4b after the existing Step 4 (clone) and before Step 5 (static analysis): (a) call `codebase_context = await get_codebase_snapshot(github_repo, github_token)`; (b) build `relevant_file_contents` by reading up to a bounded set of source files from `cloned.workspace_path` (reuse the same simple directory-walk-and-read approach already used elsewhere in the dev pipeline for `relevant_file_contents` — read text files under common source dirs, skip binary/large files, cap total content size to avoid oversized prompts, e.g. skip files >50_000 chars and stop after 20 files); (c) call `file_changes = generate_unit_tests(issue_key, issue_summary, issue_description, codebase_context, relevant_file_contents)`; (d) for each `FileChange` in `file_changes`, apply the exact pr_creator.py path-traversal guard (`workspace_root = pathlib.Path(cloned.workspace_path).resolve()`; `resolved = (workspace_root / change.path).resolve()`; raise `ValueError` if `not str(resolved).startswith(str(workspace_root) + "/")`) — catch that `ValueError` per-file (log a warning, skip that file, continue with remaining files) rather than letting it escape Step 4b, since a single malformed LLM path must not abort the whole run; (e) for each successfully-written file, build a `ToolchainCommand(name="pytest", command=["docker", "run", "--rm", "-v", f"{cloned.workspace_path}:/workspace", os.environ.get("QA_SANDBOX_IMAGE", "qa-sandbox"), "pytest", f"/workspace/{change.path}", "-v"])` and call `unit_test_results.append(run_command(cmd))`; (f) if `file_changes` is empty, set `unit_test_results = []` and continue (no execution). Update `_format_static_analysis_comment` (rename to `_format_qa_comment(unit_test_results: list[TestResult], static_results: list[TestResult], issue_key: str) -> str`) to render two labeled sections — "Unit Tests:" (or a "no unit tests generated" note when `unit_test_results` is empty) and "Static Analysis:" (existing PASSED/FAILED/TIMED OUT per-tool formatting, reused as-is) — satisfying QAREP-01's "per-category... at minimum: unit tests and static analysis" requirement. Update the call site in Step 5/6 to call `_format_qa_comment(unit_test_results, static_results, issue_key)` instead of the old single-list call. Update the module docstring's "Phase 24 will add LLM-generated unit tests" line to state this is now implemented, and add a new threat-mitigation entry `T-24-01: FileChange.path is resolved against workspace_root and rejected (ValueError, caught and skipped) if it escapes the workspace — mirrors pr_creator.py's T-15-06 guard exactly, preventing the LLM from writing outside the cloned repo.`
  </action>
  <acceptance_criteria>
    - `grep -n "generate_unit_tests" backend/services/qa_pipeline.py` matches
    - `grep -n "workspace_root" backend/services/qa_pipeline.py` matches (path-traversal guard present)
    - `grep -n "_format_qa_comment" backend/services/qa_pipeline.py` matches (renamed formatter, both old call sites updated)
    - `pytest backend/tests/test_qa_pipeline.py -x -q` passes including all pre-existing tests plus new ones from this task
    - A test asserts that a `FileChange` with a `../`-escaping path is rejected without raising out of `run()`
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_qa_pipeline.py -x -q</automated>
  </verify>
  <done>qa_pipeline.run() now fetches codebase.md context, calls generate_unit_tests with grounded repo content, safely writes any generated test file inside the cloned workspace (rejecting path-traversal attempts), executes it through the existing sandboxed test_executor machinery, and posts a single Jira comment with separate Unit Tests and Static Analysis sections — with full test coverage of the new step including the empty-generation and path-traversal-rejection edge cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloned repo content → LLM prompt | Untrusted repository file contents and `.hermes/codebase.md` are interpolated into the freellmapi prompt in `test_generator.py` |
| LLM response → filesystem write | The LLM's `### FILE:` output (file paths + content) is parsed and written to disk inside the cloned workspace by `qa_pipeline.py` before execution |
| Generated test file → subprocess execution | The written file path is passed into a `docker run` command list executed via `subprocess.run()` inside `test_executor.run_command` |

## Artifacts this phase produces

| Symbol | Kind | Location | Provides |
|--------|------|----------|----------|
| `generate_unit_tests(issue_key, issue_summary, issue_description, codebase_context, relevant_file_contents)` | function | `backend/services/test_generator.py` | LLM-grounded pytest test-file generation via freellmapi (TESTGEN-01) |
| `services.test_generator` module | module | `backend/services/test_generator.py` | New service module (does not redefine `FileChange`/`_parse_file_changes` — imports from `code_generator`) |
| `_format_qa_comment(unit_test_results, static_results, issue_key)` | function | `backend/services/qa_pipeline.py` | Replaces `_format_static_analysis_comment`; renders combined per-category Jira comment (QAREP-01) |
| `workspace_root` / path-traversal guard block | code block | `backend/services/qa_pipeline.py` (inside `run()`) | Rejects `FileChange.path` values that resolve outside the cloned workspace (T-24-01) |
| `unit_test_results` | local variable | `backend/services/qa_pipeline.py` (inside `run()`) | List of `TestResult` from executing generated pytest file(s) |
| `T-24-01` .. `T-24-06` | threat IDs | `backend/services/qa_pipeline.py` / `backend/services/test_generator.py` docstrings | New threat-mitigation entries for this phase |
| `backend/tests/test_test_generator.py` | test file | new | Unit tests for `generate_unit_tests()` |
| New test functions in `backend/tests/test_qa_pipeline.py` | test functions | modified file | Coverage for the new generation/write/execute/report step in `run()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | qa_pipeline.py FileChange write | mitigate | Resolve each `FileChange.path` against `pathlib.Path(workspace_path).resolve()`; reject (catch `ValueError`, log, skip) any path resolving outside the workspace root — exact pattern from pr_creator.py's existing T-15-06 guard |
| T-24-02 | Tampering | test_generator.py prompt construction | mitigate | Only `issue_key`/`issue_summary`/`issue_description`/`codebase_context`/`relevant_file_contents` are interpolated into the prompt; no credential or token values are ever passed into `generate_unit_tests()` (mirrors code_generator.py's T-06-01) |
| T-24-03 | Elevation of Privilege | docker run command for generated pytest file | mitigate | The generated test file path is only ever placed inside a list-form `subprocess.run()` argument (never `shell=True`), and the docker container mounts only the already-path-validated workspace directory — same sandbox boundary already established in Phase 23 (T-23-01) |
| T-24-04 | Denial of Service | LLM prompt size / generated test execution | mitigate | `relevant_file_contents` reading is capped (file count and per-file size) before being placed into the prompt; generated-test execution reuses `run_command`'s existing 120s hard timeout (T-23-02) — no new unbounded operation introduced |
| T-24-05 | Information Disclosure | Jira comment with unit test output | accept | Test stderr/stdout is truncated the same way static-analysis output already is (`_format_qa_comment` reuses the existing 500-char truncation); generated test code does not have access to credentials so cannot leak them through assertion output |
| T-24-06 | Repudiation | freellmapi call failure during test generation | mitigate | `route_request` already returns a `"[stub]"` graceful-degradation response on freellmapi failure (llm_router.py); `_parse_file_changes` (reused from code_generator.py) returns `[]` for stub/empty output, and qa_pipeline.py's empty-list branch posts a "no unit tests generated" note rather than silently doing nothing |
</threat_model>

<verification>
- `pytest backend/tests/test_test_generator.py backend/tests/test_qa_pipeline.py -x -q` passes with zero failures
- `grep -c 'shell=True' backend/services/qa_pipeline.py backend/services/test_generator.py` shows only docstring/comment occurrences (verify with the same docstring-skip logic `test_no_shell_true_in_qa_pipeline` already uses)
- Manual trace: `run()` → `get_codebase_snapshot` → `generate_unit_tests` → path-guard write → `run_command` → `_format_qa_comment` → `hermes_post_comment`, confirmed present in qa_pipeline.py via grep for each symbol
</verification>

<success_criteria>
1. `generate_unit_tests()` exists, calls freellmapi via the existing `testgen` heavy-stage route, and is grounded in cloned-repo file contents plus `.hermes/codebase.md` content, parsed via the established `### FILE:` convention
2. Generated unit tests are written into the cloned workspace (with path-traversal rejection) and executed via the existing sandboxed `test_executor` machinery, producing a structured `TestResult`
3. A single Jira comment is posted after execution containing distinct Unit Tests and Static Analysis sections, satisfying QAREP-01's per-category minimum
</success_criteria>

<output>
Create `.planning/phases/24-test-generation/24-01-SUMMARY.md` when done
</output>
