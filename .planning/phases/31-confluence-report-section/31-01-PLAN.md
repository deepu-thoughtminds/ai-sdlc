---
phase: 31-confluence-report-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/sonar_scanner.py
  - backend/services/confluence_client.py
  - backend/services/qa_pipeline.py
  - backend/tests/test_sonar_scanner.py
  - backend/tests/test_confluence_client.py
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
must_haves:
  truths:
    - "SONAR_URL absent or sonar scan failed/skipped → Confluence page shows graceful fallback note, no error raised (REPORT-03)"
    - "SonarQube measures API call failure (network, timeout, HTTP error) → treated as unavailable; Confluence page still publishes with fallback section (REPORT-03)"
    - "Dashboard deep link uses format {sonar_url}/dashboard?id={project_key} (REPORT-02)"
    - "coverage metric absent in API response → rendered as N/A (REPORT-02)"
    - "alert_status OK renders as PASSED; ERROR renders as FAILED (REPORT-02)"
    - "All metrics fetched in a single /api/measures/component request (REPORT-02)"
    - "sonar_metrics parameter to publish_qa_report() is Optional — existing callers without it continue to work (REPORT-01)"
  artifacts:
    - path: "backend/services/sonar_scanner.py"
      provides: "SonarMetrics dataclass and fetch_sonar_metrics() function"
      contains: "class SonarMetrics"
    - path: "backend/services/confluence_client.py"
      provides: "_render_sonar_section() and updated publish_qa_report() signature"
      contains: "_render_sonar_section"
    - path: "backend/tests/test_sonar_scanner.py"
      provides: "Tests for fetch_sonar_metrics()"
      contains: "test_fetch_sonar_metrics"
    - path: "backend/tests/test_confluence_client.py"
      provides: "Tests for _render_sonar_section()"
      contains: "test_render_sonar_section"
  key_links:
    - from: "backend/services/qa_pipeline.py"
      to: "backend/services/sonar_scanner.py"
      via: "fetch_sonar_metrics() called after sonar_result.returncode == 0"
      pattern: "fetch_sonar_metrics"
    - from: "backend/services/qa_pipeline.py"
      to: "backend/services/confluence_client.py"
      via: "sonar_metrics passed to publish_qa_report(sonar_metrics=sonar_metrics)"
      pattern: "publish_qa_report.*sonar_metrics"
---

<objective>
Add SonarMetrics dataclass and fetch_sonar_metrics() to sonar_scanner.py, update
publish_qa_report() in confluence_client.py to render a SonarQube HTML section
(quality gate, metrics table, dashboard link, or graceful fallback), and wire the
fetch + render through qa_pipeline.py.

Purpose: Satisfies REPORT-01..03 — every Confluence QA page gets a SonarQube section
with actionable metrics and a dashboard link, or a clear fallback when the scan did
not run.

Output:
- SonarMetrics dataclass + fetch_sonar_metrics() in sonar_scanner.py
- _render_sonar_section() helper + updated publish_qa_report() in confluence_client.py
- fetch + pass-through wiring in qa_pipeline.py
- Tests in test_sonar_scanner.py and test_confluence_client.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/sonar_scanner.py
@backend/services/confluence_client.py
@backend/services/qa_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SonarMetrics dataclass and fetch_sonar_metrics() to sonar_scanner.py</name>
  <files>backend/services/sonar_scanner.py, backend/tests/test_sonar_scanner.py</files>
  <behavior>
    - fetch_sonar_metrics(project_key, sonar_url, token) with valid measures JSON → SonarMetrics with gate_status="PASSED", correct int/float fields, dashboard_url="{sonar_url}/dashboard?id={project_key}"
    - alert_status "ERROR" in response → gate_status="FAILED"
    - measures list missing the "coverage" metric entirely → SonarMetrics.coverage is None
    - httpx.RequestError raised → returns None (never raises)
    - HTTP 500 response → returns None (never raises)
    - Missing "component" key in response JSON → returns None (never raises)
  </behavior>
  <action>
Add to backend/services/sonar_scanner.py (after existing imports, before _read_ce_task_id):

1. Add `from dataclasses import dataclass` to the imports block.

2. Add the SonarMetrics dataclass:

    @dataclass
    class SonarMetrics:
        gate_status: str        # "PASSED" or "FAILED"
        bugs: int
        vulnerabilities: int
        code_smells: int
        coverage: float | None  # None when not measured (no tests)
        duplications: float
        dashboard_url: str

3. Add fetch_sonar_metrics() immediately after the dataclass:

    def fetch_sonar_metrics(
        project_key: str, sonar_url: str, token: str
    ) -> "SonarMetrics | None":
        """Fetch quality metrics from SonarQube measures API. Returns None on any failure.

        Single GET to /api/measures/component with all required metricKeys.
        Never raises — all exceptions are caught and logged as warnings.
        Dashboard URL: {sonar_url}/dashboard?id={project_key}
        """
        metric_keys = "alert_status,bugs,vulnerabilities,code_smells,coverage,duplicated_lines_density"
        try:
            r = httpx.get(
                f"{sonar_url}/api/measures/component",
                params={"component": project_key, "metricKeys": metric_keys},
                headers={"Authorization": f"Bearer {token}"},
                timeout=10.0,
            )
            r.raise_for_status()
            measures_list = r.json()["component"]["measures"]
            # Build a lookup dict: metric name → value string
            by_metric = {m["metric"]: m.get("value", "") for m in measures_list}

            raw_status = by_metric.get("alert_status", "ERROR")
            gate_status = "PASSED" if raw_status == "OK" else "FAILED"

            coverage_raw = by_metric.get("coverage")
            coverage = float(coverage_raw) if coverage_raw not in (None, "") else None

            return SonarMetrics(
                gate_status=gate_status,
                bugs=int(by_metric.get("bugs", "0")),
                vulnerabilities=int(by_metric.get("vulnerabilities", "0")),
                code_smells=int(by_metric.get("code_smells", "0")),
                coverage=coverage,
                duplications=float(by_metric.get("duplicated_lines_density", "0")),
                dashboard_url=f"{sonar_url}/dashboard?id={project_key}",
            )
        except Exception:
            logger.warning(
                "fetch_sonar_metrics failed for project_key=%s — returning None", project_key
            )
            return None

In backend/tests/test_sonar_scanner.py, add tests for fetch_sonar_metrics() in a new
TestFetchSonarMetrics class. Use `unittest.mock.patch("httpx.get", ...)` or
`unittest.mock.MagicMock`. Tests must cover all six behavior cases above.
For the happy-path mock, return a MagicMock with status_code=200, raise_for_status=MagicMock(),
and json() returning:
{"component": {"measures": [
  {"metric": "alert_status", "value": "OK"},
  {"metric": "bugs", "value": "3"},
  {"metric": "vulnerabilities", "value": "1"},
  {"metric": "code_smells", "value": "10"},
  {"metric": "coverage", "value": "82.5"},
  {"metric": "duplicated_lines_density", "value": "4.2"}
]}}
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -m pytest tests/test_sonar_scanner.py -k "fetch_sonar" -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All fetch_sonar_metrics tests pass. SonarMetrics dataclass importable from
    services.sonar_scanner. fetch_sonar_metrics returns None on any exception, not raise.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _render_sonar_section() and update publish_qa_report() in confluence_client.py</name>
  <files>backend/services/confluence_client.py, backend/tests/test_confluence_client.py</files>
  <behavior>
    - _render_sonar_section(None) → returned string contains "SonarQube scan unavailable" and an h2 header (REPORT-03)
    - _render_sonar_section(SonarMetrics with gate_status="PASSED") → contains "PASSED", bug count, vuln count, code smell count, duplications, and the dashboard_url in an anchor tag (REPORT-02)
    - _render_sonar_section(SonarMetrics with coverage=None) → contains "N/A" for coverage (REPORT-02)
    - _render_sonar_section(SonarMetrics with coverage=82.5) → contains "82.5" (REPORT-02)
    - publish_qa_report() called without sonar_metrics arg → does not error (backward compat, REPORT-01)
    - publish_qa_report() called with sonar_metrics=None → page body still contains a SonarQube h2 section (REPORT-01 + REPORT-03)
    - publish_qa_report() called with SonarMetrics → page body contains dashboard link (REPORT-01 + REPORT-02)
  </behavior>
  <action>
In backend/services/confluence_client.py:

1. Add import at top of file (alongside existing imports):
   from services.sonar_scanner import SonarMetrics

   Use TYPE_CHECKING guard if needed to avoid circular import. Since sonar_scanner
   imports from test_executor (not from confluence_client), there is no circular import —
   direct import is fine.

2. Add _render_sonar_section() as a module-level private function alongside _escape(),
   _render_text_only_template(), _render_diagram_template():

    def _render_sonar_section(sonar_metrics: "SonarMetrics | None") -> str:
        """Render the SonarQube HTML section for the Confluence QA report.

        When sonar_metrics is None (scan skipped or failed), returns a graceful
        unavailable note (REPORT-03). When metrics are present, returns a table
        with quality gate, per-metric counts, and a dashboard deep link (REPORT-02).
        """
        if sonar_metrics is None:
            return "<h2>SonarQube</h2><p>SonarQube scan unavailable.</p>"

        coverage_display = (
            f"{sonar_metrics.coverage:.1f}%" if sonar_metrics.coverage is not None else "N/A"
        )
        return (
            "<h2>SonarQube</h2>"
            "<table>"
            "<tr><th>Metric</th><th>Value</th></tr>"
            f"<tr><td>Quality Gate</td><td>{_escape(sonar_metrics.gate_status)}</td></tr>"
            f"<tr><td>Bugs</td><td>{sonar_metrics.bugs}</td></tr>"
            f"<tr><td>Vulnerabilities</td><td>{sonar_metrics.vulnerabilities}</td></tr>"
            f"<tr><td>Code Smells</td><td>{sonar_metrics.code_smells}</td></tr>"
            f"<tr><td>Coverage</td><td>{coverage_display}</td></tr>"
            f"<tr><td>Duplications</td><td>{sonar_metrics.duplications:.1f}%</td></tr>"
            "</table>"
            f'<p><a href="{sonar_metrics.dashboard_url}">View SonarQube Dashboard</a></p>'
        )

3. Update ConfluenceClient.publish_qa_report() signature to:
   async def publish_qa_report(
       self, project: Project, issue_key: str, report_text: str,
       remediation_html: str = "", sonar_metrics: "SonarMetrics | None" = None
   ) -> str:

   Inside the method body, locate where body_html is assembled. The current form is:
       body_html = f"<h1>QA Report: {issue_key}</h1><pre>{_escape(report_text)}</pre>" + remediation_html
   Change it to insert the sonar section between the pre block and remediation_html:
       body_html = (
           f"<h1>QA Report: {issue_key}</h1>"
           f"<pre>{_escape(report_text)}</pre>"
           + _render_sonar_section(sonar_metrics)
           + remediation_html
       )

4. Update the module-level publish_qa_report() wrapper function similarly:
   async def publish_qa_report(
       project: Project, issue_key: str, report_text: str,
       remediation_html: str = "", sonar_metrics: "SonarMetrics | None" = None
   ) -> str:
   Pass sonar_metrics through to client.publish_qa_report().

In backend/tests/test_confluence_client.py, add a TestRenderSonarSection class that
imports _render_sonar_section from services.confluence_client and SonarMetrics from
services.sonar_scanner. Tests must cover all seven behavior cases above. For
publish_qa_report integration tests, patch hermes_client helpers with AsyncMock
(same pattern as existing tests in that file) and assert the page body contains
expected strings.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -m pytest tests/test_confluence_client.py -k "sonar" -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All _render_sonar_section and publish_qa_report sonar tests pass. Existing
    confluence_client tests still pass (backward compat). The function renders
    "SonarQube scan unavailable" for None and a table with dashboard link for metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire fetch_sonar_metrics() into qa_pipeline.py</name>
  <files>backend/services/qa_pipeline.py</files>
  <action>
In backend/services/qa_pipeline.py:

1. Add to the imports block (near the existing sonar_scanner import or confluence_client import):
   from services.sonar_scanner import fetch_sonar_metrics

2. After line ~541 where sonar_result is assigned:
   sonar_result = _run_sonar_step(cloned, compose_network, issue_key)

   Immediately after, add the metrics fetch block:
   # Fetch SonarQube quality metrics if scan succeeded (REPORT-02).
   # Read env vars again — cheap, avoids refactoring _run_sonar_step return type.
   sonar_metrics = None
   if sonar_result is not None and sonar_result.returncode == 0:
       _sonar_url = os.environ.get("SONAR_URL", "")
       _sonar_token = os.environ.get("SONAR_TOKEN", "")
       if _sonar_url and _sonar_token:
           _project_key = f"{cloned.owner}__{cloned.repo}"
           sonar_metrics = fetch_sonar_metrics(_project_key, _sonar_url, _sonar_token)

3. Update the publish_qa_report() call at line ~647 to pass sonar_metrics:
   qa_page_url = await publish_qa_report(
       project, issue_key, comment_text, remediation_html, sonar_metrics=sonar_metrics
   )

   Note: sonar_metrics must be defined in the outer scope. If the exception branch
   (lines ~613-631) is hit, sonar_metrics will not be set; initialize it to None
   near the top of the try block (same area where other local variables like
   autofix_pr_url are initialized). Check where those are initialized and add:
   sonar_metrics = None
   alongside them, before the try block that runs the pipeline steps.

No other changes needed. The existing _run_sonar_step function is unchanged.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -m pytest tests/test_sonar_scanner.py tests/test_confluence_client.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    qa_pipeline.py imports fetch_sonar_metrics without error. sonar_metrics is
    fetched after a successful scan and passed to publish_qa_report(). All existing
    qa_pipeline tests still pass. sonar_metrics=None when scan was skipped or failed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qa_pipeline → SonarQube API | fetch_sonar_metrics() calls SonarQube over internal Docker network; response is external data |
| fetch_sonar_metrics → confluence_client | SonarMetrics fields are rendered into HTML; gate_status goes through _escape() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | fetch_sonar_metrics() | mitigate | SONAR_TOKEN read from env var, passed as Bearer header, never logged in success path; warning log on failure uses project_key only |
| T-31-02 | Tampering | _render_sonar_section() HTML output | mitigate | gate_status goes through _escape() before interpolation; int/float fields are typed values (not raw strings from API), not escapable but also not XSS vectors; dashboard_url is constructed from env var (SONAR_URL) + project_key — not from API response |
| T-31-03 | Denial of Service | fetch_sonar_metrics() httpx.get | mitigate | timeout=10.0 hardcoded; all exceptions caught and return None — QA pipeline publish is never blocked by sonar metrics fetch failure |
</threat_model>

<verification>
Full phase verification:

```bash
cd /home/deepu/thoughtminds_projects/ai-sdlc/backend
python -m pytest tests/test_sonar_scanner.py tests/test_confluence_client.py -v
```

Spot-check import chain is clean:
```bash
cd /home/deepu/thoughtminds_projects/ai-sdlc/backend
python -c "from services.sonar_scanner import SonarMetrics, fetch_sonar_metrics; print('OK')"
python -c "from services.confluence_client import _render_sonar_section; print('OK')"
```
</verification>

<success_criteria>
1. REPORT-01: `publish_qa_report()` HTML output always includes a SonarQube h2 section, whether metrics are present or not.
2. REPORT-02: When scan succeeded, section displays quality gate (PASSED/FAILED), bugs, vulnerabilities, code smells, coverage (or N/A), duplications, and a hyperlink to the SonarQube dashboard at `{SONAR_URL}/dashboard?id={project_key}`.
3. REPORT-03: When sonar_metrics is None (scan skipped/failed/API error), section shows "SonarQube scan unavailable" rather than an empty block or error.
4. All existing tests in test_sonar_scanner.py and test_confluence_client.py continue to pass (backward compat).
5. fetch_sonar_metrics() never raises — returns None on any failure, confirmed by test cases for network error and HTTP 500.
</success_criteria>

<output>
Create `.planning/phases/31-confluence-report-section/31-01-SUMMARY.md` when done.
</output>
