---
phase: 34
plan: "34-01"
title: "Architecture Pipeline: opencode CLI + Graph Context Migration"
goal: "Route architecture pipeline through opencode CLI with cbm graph context, retiring route_request() direct HTTP calls"
requirements: [AGENT-05, CTX-03]
wave: 1
depends_on: []
files_modified:
  - backend/services/architecture_pipeline.py
  - backend/tests/test_architecture_pipeline.py
autonomous: true
status: pending
must_haves:
  truths:
    - "architecture_pipeline.py imports neither route_request nor get_codebase_snapshot"
    - "_run_opencode_arch() invokes opencode CLI subprocess (mirrors _run_opencode_describe)"
    - "_query_arch_context() calls cbm_call('search_graph', ...) and returns graph node text"
    - "Both _run_complex() and _run_simple() call _run_opencode_arch() instead of route_request()"
    - "run() calls _query_arch_context() instead of get_codebase_snapshot()"
    - "All 7 tests pass with mocks updated to patch _run_opencode_arch and cbm_call instead of route_request and get_codebase_snapshot"
  artifacts:
    - path: "backend/services/architecture_pipeline.py"
      provides: "Migrated architecture pipeline — opencode CLI + cbm graph"
    - path: "backend/tests/test_architecture_pipeline.py"
      provides: "Updated tests mocking new functions"
  key_links:
    - from: "architecture_pipeline._run_complex/_run_simple"
      to: "_run_opencode_arch"
      via: "replaces 2x route_request('architecture', prompt) calls"
    - from: "architecture_pipeline.run"
      to: "_query_arch_context"
      via: "replaces get_codebase_snapshot() call at ~line 179"
---

<objective>
Migrate architecture_pipeline.py from direct opencode.ai HTTP calls (via route_request) and GitHub snapshot reads (via get_codebase_snapshot) to the Phase 33 pattern: cbm_call for graph context and opencode CLI subprocess for LLM inference.

Purpose: Fulfil AGENT-05 (no direct opencode.ai API calls in architecture pipeline) and CTX-03 (graph queried before architecture pipeline runs).
Output: Updated architecture_pipeline.py and test_architecture_pipeline.py with no route_request or get_codebase_snapshot imports remaining.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/describe_pipeline.py
@backend/services/cbm_client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _run_opencode_arch() and _query_arch_context() to architecture_pipeline.py</name>
  <files>backend/services/architecture_pipeline.py</files>
  <behavior>
    - _run_opencode_arch(prompt) returns tuple[str, str] — (content, "") — mirrors _run_opencode_describe exactly (same subprocess pattern, same env setup via _opencode_config(), same JSON event parsing, same 120s timeout, same graceful degradation to ("", ""))
    - _query_arch_context(project_id, issue_key, issue_summary) calls asyncio.to_thread(cbm_call, "search_graph", {"query": issue_summary, "limit": 20}) — returns graph node text formatted as "- name (file)" lines, or "(no codebase graph context available)" on empty/error
    - Both functions are async
  </behavior>
  <action>
    At the top of architecture_pipeline.py, add imports for asyncio, json, os, tempfile (already present — confirm before adding), and add:
      from services.agentic_coder import _opencode_config
      from services.cbm_client import cbm_call

    Remove (or leave unused temporarily — Task 2 cleans them):
      from services.codebase_snapshot_reader import get_codebase_snapshot
      from services.llm_router import route_request

    Add _run_opencode_arch() directly below the module docstring / imports block, copying _run_opencode_describe verbatim from describe_pipeline.py but renaming it and changing the log tag from "describe" to "arch".

    Add _query_arch_context(project_id: int, issue_key: str, issue_summary: str) -> str as an async function that calls cbm_call via asyncio.to_thread, parses nodes (same pattern as describe_pipeline.py lines 142-149), and returns the graph text string. Logs a warning on exception and returns fallback string.

    Per CTX-03: the cbm query uses the issue_summary as the search query with limit=20.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -c "from services.architecture_pipeline import _run_opencode_arch, _query_arch_context; print('imports OK')"</automated>
  </verify>
  <done>Both functions importable; no NameError; no route_request or get_codebase_snapshot imported at module level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace route_request and get_codebase_snapshot call sites in architecture_pipeline.py</name>
  <files>backend/services/architecture_pipeline.py</files>
  <behavior>
    - run(): calls _query_arch_context(project.id, issue_key, issue_summary) instead of get_codebase_snapshot(github_repo, github_token); stores result as graph_text (str, never None — no truncation needed, cbm returns concise node list)
    - run(): passes graph_text to _run_complex() and _run_simple() as codebase_text parameter (rename snapshot param to codebase_text in both functions)
    - _run_complex(): replaces `route_result = route_request("architecture", prompt)` with `content, _ = await _run_opencode_arch(prompt)` then `reasoning, answer = split_reasoning(content)` (drop the `if route_result.reasoning:` branch — opencode CLI has no separate reasoning field)
    - _run_simple(): same replacement as _run_complex() — one route_request call at ~line 485 becomes _run_opencode_arch
    - Prompt strings: replace `"Codebase context (.hermes/codebase.md snapshot):\n{codebase_text}\n\n"` with `"Codebase context (module graph):\n{codebase_text}\n\n"` in both _run_complex and _run_simple
    - Remove the github_token / github_repo decrypt block from run() that was only used for get_codebase_snapshot (lines ~159-177) — those credentials are not needed by cbm_call
    - Remove all snapshot-related safe_record_agent_event calls and replace with a graph context record (detail="graph context loaded" or "no graph context")
    - Remove from imports: get_codebase_snapshot, route_request
    - The LLMResponse import from llm_router can be removed if no longer used anywhere in the file; check with grep first
  </behavior>
  <action>
    Make the following targeted changes to architecture_pipeline.py:

    1. In run() (~line 179): replace the `snapshot = await get_codebase_snapshot(...)` block and the preceding github_token/github_repo decrypt block with a single call:
       `graph_text = await _query_arch_context(project.id, issue_key, issue_summary)`
       Update safe_record_agent_event detail to reference graph_text instead of snapshot.

    2. In run() (~line 186): the call to classify_complexity already passes `codebase_snapshot=snapshot` — update to `codebase_snapshot=graph_text` (classify_complexity still reads snapshot for its own prompt).

    3. In run() (~lines 204, 208): _run_complex and _run_simple are called with `snapshot` param — rename to pass graph_text, and update function signatures to accept `codebase_text: str` instead of `snapshot: str | None`.

    4. In _run_complex() (~line 308): replace `codebase_text = snapshot[:8000] if snapshot else "(no codebase context available)"` with just `codebase_text = codebase_text` (it's already a str from _query_arch_context). Update prompt label per AGENT-05 (D-nn is not cited here — this is a direct requirement change).

    5. In _run_complex() (~line 346): replace:
       ```
       route_result = route_request("architecture", prompt)
       reasoning, answer = split_reasoning(route_result.content)
       if route_result.reasoning:
           reasoning = route_result.reasoning
       ```
       with:
       ```
       content, _ = await _run_opencode_arch(prompt)
       reasoning, answer = split_reasoning(content)
       ```
       Make _run_complex() async if it is not already (add `async` keyword to def).

    6. In _run_simple() (~line 458): same codebase_text swap as step 4.

    7. In _run_simple() (~line 485): same route_result replacement as step 5.
       Make _run_simple() async if not already.

    8. Remove `from services.codebase_snapshot_reader import get_codebase_snapshot` from imports.

    9. Remove `from services.llm_router import route_request` from imports (grep first to confirm no other caller in this file).

    10. In run(), update the two calls to _run_complex() and _run_simple() to use `await` (they become coroutines).

    Per T-21-01: no decrypted token values are logged anywhere in this file. The github_token/github_repo decrypt block is removed entirely (not needed for cbm_call). cbm_call does not receive any project credentials.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -c "import ast, pathlib; ast.parse(pathlib.Path('services/architecture_pipeline.py').read_text()); print('syntax OK')" && grep -c "route_request\|get_codebase_snapshot" services/architecture_pipeline.py | grep -E "^0$" && echo "no stale imports"</automated>
  </verify>
  <done>File parses clean; grep confirms route_request and get_codebase_snapshot do not appear anywhere in architecture_pipeline.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update test_architecture_pipeline.py to mock new functions</name>
  <files>backend/tests/test_architecture_pipeline.py</files>
  <behavior>
    - All patches for "services.architecture_pipeline.route_request" replaced with patches for "services.architecture_pipeline._run_opencode_arch" (AsyncMock returning tuple[str, str])
    - All patches for "services.architecture_pipeline.get_codebase_snapshot" replaced with patches for "services.architecture_pipeline._query_arch_context" (AsyncMock returning str graph_text)
    - test_run_calls_llm_with_architecture_stage: rename to test_run_calls_opencode_arch; assert _run_opencode_arch was called (not route_request); check prompt still contains issue_summary
    - test_run_passes_snapshot_content_to_prompt: rename to test_run_passes_graph_context_to_prompt; mock _query_arch_context to return sentinel string; assert that sentinel string appears in prompt passed to _run_opencode_arch
    - test_run_passes_snapshot_to_complexity_classifier: still valid — _query_arch_context result is passed as codebase_snapshot to classify_complexity; update patch targets accordingly
    - Remove LLMResponse import from llm_router since stub helpers no longer need it; replace _make_stub_llm_response_complex() and _make_stub_llm_response_simple() with helpers that return plain tuples: ("## Summary\n...\n## Approach\n...", "") matching the (content, reasoning) tuple from _run_opencode_arch
    - T-04-01 check: still verify that "jira-secret-token" and "conf-secret-token" do not appear in the prompt; the check now reads prompt from _run_opencode_arch mock call_args, not route_request
  </behavior>
  <action>
    Rewrite test_architecture_pipeline.py:

    1. Remove `from services.llm_router import LLMResponse` import.

    2. Replace _make_stub_llm_response_complex() with _make_stub_arch_response_complex() returning:
       ("## Summary\nThis is a complex multi-service architecture.\n## Approach\nUse microservices with async messaging.\n## Component Breakdown\nAPI Gateway, Auth Service, User Service\n## Integration Points\nREST between gateway and services; Kafka for events\n## Key Decisions\nUse event sourcing for audit trail\n## Risks\nHigh coupling between services if not decoupled properly", "")

    3. Replace _make_stub_llm_response_simple() with _make_stub_arch_response_simple() returning:
       ("## Summary\nSimple single-service change.\n## Approach\nAdd a field to the existing User model.\n## Key Decisions\nMinimal change — no new services needed\n## Risks\nLow risk; backward compatible", "")

    4. In every test, replace:
       - patch("services.architecture_pipeline.route_request", return_value=_make_stub_llm_response_complex()) → patch("services.architecture_pipeline._run_opencode_arch", new_callable=AsyncMock, return_value=_make_stub_arch_response_complex())
       - patch("services.architecture_pipeline.get_codebase_snapshot", new_callable=AsyncMock, return_value="...") → patch("services.architecture_pipeline._query_arch_context", new_callable=AsyncMock, return_value="- AuthService (backend/services/auth.py)\n- UserModel (backend/models/user.py)")

    5. Rename test_run_calls_llm_with_architecture_stage → test_run_calls_opencode_arch_for_architecture_stage; update assertion to check mock_opencode_arch.called and that issue_summary appears in the prompt arg.

    6. In test_run_passes_snapshot_content_to_prompt (or equivalent): patch _query_arch_context to return "SENTINEL_COMPONENT: backend/services/my_unique_module.py"; assert that string appears in _run_opencode_arch call_args prompt.

    7. Docstring in the file header: update tests list to reflect _run_opencode_arch and _query_arch_context mocking instead of route_request and get_codebase_snapshot.
  </action>
  <verify>
    <automated>cd /home/deepu/thoughtminds_projects/ai-sdlc/backend && python -m pytest tests/test_architecture_pipeline.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass (exit 0); no import of route_request or LLMResponse in test file; no patch of get_codebase_snapshot or route_request in any test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture_pipeline → opencode CLI | Prompt text (ticket summary/description) crosses to subprocess via argv |
| architecture_pipeline → cbm_call | issue_summary crosses to cbm CLI subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | prompt construction in _run_complex/_run_simple | mitigate | issue_summary/description truncated at 2000/4000 chars (T-WR-02 carried forward); cbm graph text capped at 20 nodes |
| T-34-02 | Information Disclosure | _query_arch_context logging | mitigate | log only issue_key, not graph node content; cbm_call receives no project credentials |
| T-34-03 | Denial of Service | opencode CLI subprocess timeout | accept | 120s timeout inherited from _run_opencode_describe; graceful degradation to ("", "") |
| T-34-SC | Tampering | no new npm/pip installs in this phase | accept | no package manager calls; cbm_client and agentic_coder already installed |
</threat_model>

<verification>
1. `python -c "from services.architecture_pipeline import run, _run_opencode_arch, _query_arch_context"` — imports clean
2. `grep -c "route_request\|get_codebase_snapshot" backend/services/architecture_pipeline.py` returns 0
3. `python -m pytest backend/tests/test_architecture_pipeline.py -x -q` — all 7 tests pass
4. `grep -c "route_request\|get_codebase_snapshot\|LLMResponse" backend/tests/test_architecture_pipeline.py` returns 0
</verification>

<success_criteria>
- AGENT-05: architecture_pipeline.py contains no import or call to route_request; opencode CLI subprocess is invoked via _run_opencode_arch() for both complexity paths
- CTX-03: run() calls _query_arch_context() which invokes cbm_call("search_graph", ...) with the ticket's issue_summary before any LLM call is made
- No 401 errors from opencode.ai direct API — the http call path is gone
- All existing tests green with updated mocks
</success_criteria>

<output>
Create `.planning/phases/34-architecture-pipeline/34-01-SUMMARY.md` when done.
</output>
